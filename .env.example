

# Copy this file to .env (or .env.local) and replace the values with your Supabase project
# settings. Restart the dev server after editing environment variables.

// PocketBase (optional) - to use PocketBase locally set VITE_USE_POCKETBASE=1 and VITE_POCKETBASE_URL
// Example:
VITE_USE_POCKETBASE=1
VITE_POCKETBASE_URL=http://127.0.0.1:8090
